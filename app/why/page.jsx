import WhyClient from '../../components/WhyClient';

export const metadata = {
  title: 'Why We Built This — CarComfortScore',
  description: 'Pakistan car reviews never objectively measure ride quality. CarComfortScore was built to change that using engineering and science.',
};

export default function WhyPage() {
  return <WhyClient />;
}
